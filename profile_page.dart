import 'package:flutter/material.dart';import '../services/app_state.dart';import '../widgets/app_header.dart';
class ProfilePage extends StatelessWidget{const ProfilePage({super.key});@override Widget build(BuildContext c)=>AnimatedBuilder(animation:AppState.instance,builder:(_,__) {final s=AppState.instance;return ListView(padding:const EdgeInsets.all(18),children:[const AppHeader(title:'حسابي والإحصائيات',subtitle:'تقدمك، إنجازاتك، إعداداتك'),const SizedBox(height:18),GridView.count(shrinkWrap:true,physics:const NeverScrollableScrollPhysics(),crossAxisCount:2,childAspectRatio:1.55,crossAxisSpacing:8,mainAxisSpacing:8,children:[_stat('المستوى','${s.level}'),_stat('XP','${s.xp}'),_stat('وقت المذاكرة','${s.totalMinutes} د'),_stat('المهام','${s.completedTasks}/${s.tasks.length}'),_stat('تقدم التعلم','${(s.learningProgress*100).round()}%'),_stat('متوسط الاختبارات','${s.averageExam.round()}%')]),const SizedBox(height:18),const Text('مراجعة الأخطاء',style:TextStyle(fontSize:20,fontWeight:FontWeight.bold)),...s.reviews.map((r)=>Card(child:ListTile(title:Text(r.topic),subtitle:Text('${r.subject} • ${r.errors} أخطاء'),trailing:r.reviewed?const Icon(Icons.check_circle):FilledButton(onPressed:()=>s.review(r),child:const Text('راجعت')))),),const SizedBox(height:18),const Text('المظهر',style:TextStyle(fontSize:20,fontWeight:FontWeight.bold)),RadioListTile(title:const Text('حسب الجهاز'),value:'system',groupValue:s.themeMode,onChanged:(v)=>s.setTheme(v!)),RadioListTile(title:const Text('فاتح'),value:'light',groupValue:s.themeMode,onChanged:(v)=>s.setTheme(v!)),RadioListTile(title:const Text('داكن'),value:'dark',groupValue:s.themeMode,onChanged:(v)=>s.setTheme(v!)),const SizedBox(height:10),const Card(child:ListTile(leading:Icon(Icons.cloud_done),title:Text('متصل بالإنترنت'),subtitle:Text('التطبيق يعتمد على Backend للذكاء الاصطناعي ويمكن توسيعه للمزامنة السحابية.'))),const Card(child:ListTile(leading:Icon(Icons.security),title:Text('الأمان'),subtitle:Text('مفتاح مزود الذكاء الاصطناعي لا يوضع داخل تطبيق Flutter؛ يحتفظ به الخادم.'))),const Card(child:ListTile(leading:Icon(Icons.devices),title:Text('الإصدارات المستهدفة'),subtitle:Text('Android • iOS • Web، مع بنية قابلة لإضافة منصات أخرى.'))),const Card(child:ListTile(leading:Icon(Icons.info_outline),title:Text('الإصدار'),subtitle:Text('Unified V2250.0.0 — consolidated implementation')))]);}
Widget _stat(String a,String b)=>Card(child:Padding(padding:const EdgeInsets.all(14),child:Column(mainAxisAlignment:MainAxisAlignment.center,children:[Text(b,style:const TextStyle(fontSize:24,fontWeight:FontWeight.w900)),const SizedBox(height:4),Text(a)])));
}
